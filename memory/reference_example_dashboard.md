---
name: reference-example-dashboard
description: Where the performance and error dashboards live.
metadata:
  type: reference
---

Performance metrics: `https://dashboards.example.com/perf`
Error tracking: `https://dashboards.example.com/errors`

(A `reference` memory is just a durable pointer — no Why/How needed. Keep the URLs current;
a stale pointer is worse than none.)
